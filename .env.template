# This file is a template for your own .env file.
# Copy it to a file named .env and fill in the values.
# For the secret key, generate a secure 64-character hexadecimal string:
# Example: openssl rand -hex 32
# Or: python -c "import secrets; print(secrets.token_hex(32))"
# Never share the resulting .env publicly.

########################################
# JWT Settings
########################################
SECRET_KEY=<your_secure_random_string>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=<your_access_token_expiry_in_minutes> # e.g., 15
REFRESH_TOKEN_EXPIRE_MINUTES=<your_refresh_token_expiry_in_minutes> # e.g., 1440 for 1 day

########################################
# PostgreSQL Database Configuration
########################################
POSTGRES_USER=<your_db_user_name>
POSTGRES_PASSWORD=<your_db_user_password>
POSTGRES_DB=<your_db_name>
POSTGRES_HOST=db # docker service name
POSTGRES_PORT=5432 # default PostgreSQL port

# Superuser credentials (used only during DB init or migrations)
POSTGRES_SUPERUSER=<your_db_superuser_name>
POSTGRES_SUPERPWD=<your_db_superuser_password>

########################################
# Redis Configuration
########################################
REDIS_HOST=redis # docker service name
REDIS_PORT=6379 # default Redis port